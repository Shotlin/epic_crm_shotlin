/**
 * Epic BOS currently opens one local desktop workspace at a time. This is a
 * durable internal identifier only; customer-facing names always come from
 * the governed kernel tenant and company records.
 */
export const PRIMARY_WORKSPACE_ID = 'tenant-northstar';

